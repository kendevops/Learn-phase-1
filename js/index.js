const loginInfo = () => {
  const userEmail = document.getElementById("email");
  const userPass = document.getElementById("password");
  const details = {
    username: userEmail,
    password: userPass
  };

  console.log(details);
};
